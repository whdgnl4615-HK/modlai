{
  "name": "modlai-backend",
  "version": "1.0.0",
  "description": "MODLai backend — AI image generation proxy + Stripe + Supabase",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "check": "node --check api/_lib/utils.js && node --check api/generate/nanobanana.js && echo 'syntax OK'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@google/generative-ai": "^0.21.0",
    "@supabase/supabase-js": "^2.45.0",
    "openai": "^4.67.0",
    "stripe": "^17.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
